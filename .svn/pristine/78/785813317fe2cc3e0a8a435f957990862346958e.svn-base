#ifndef _VELIB_PLATFORM_CONSOLE_H_
#define _VELIB_PLATFORM_CONSOLE_H_

// Platform specific things which make only sense if a console is present
#include <velib/base/base.h>
#include <velib/utils/ve_getopt.h>

extern struct option consoleOptions[];

void	consoleUsage(char* program);
veBool	consoleOption(int flag);
veBool	consoleArgs(int argc, char *argv[]);

void	pltExit(int ret);
void	pltOptionsUsage(void);

#endif

