#include <velib/base/base.h>
#include <velib/mk2/hal.h>

/*
 * Embedded products can typically control the power of the MK2.
 * PC don't have those capability, so dummy functions are provided.
 */
void mk2IoInit(void)
{
}

void mk2HoldInReset(veBool inReset)
{
	VE_UNUSED(inReset);
}

void mk2EnsureVebusPower(veBool power)
{
	VE_UNUSED(power);
}
